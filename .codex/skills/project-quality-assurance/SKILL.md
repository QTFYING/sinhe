---
name: project-quality-assurance
description: 作为本项目的最终质量闸门使用。完成一批接口、模型或业务模块后，对多租户隔离、金额正确性、状态机自洽、幂等策略、文档一致性和遗留旧字段残留做集中复核时使用。
---

# 项目质量闸门

对一批改动做最终一致性复核。

## 适用场景

- 完成一批接口、模型或业务模块后。
- 特别适用于订单、导入、支付、核销、多租户边界等主链路改动。

## 必做顺序

1. 先对照 `docs/api`、`enums`、`contracts`、`data-model-reference` 检查是否漂移。
2. 再检查多租户隔离、金额口径、状态机和幂等是否自洽。
3. 再检查是否残留旧字段、旧接口、旧状态或废弃链路。
4. 能跑验证时，至少执行与改动匹配的 build、smoke 和回归测试；不能跑就明确说明。

## 硬约束

- 发现事实源冲突时，按 `docs/api -> enums -> contracts -> data-model-reference -> code -> Swagger` 回溯。
- 旧字段名与新字段名同时存在，默认视为风险。
- 订单状态、支付状态、账务流水互相打架，默认视为风险。
- 废弃链路重新进入主流程，默认视为风险。
- 不能把“能编译”当成“已验收”。

## 完成标准

- 结论能说明“为何正确”或“哪里有风险”。
- 主要业务红线没有明显漏洞。
- 测试执行情况和剩余风险已说明清楚。
